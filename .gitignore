# Python
__pycache__/
*.py[cod]
.venv/
venv/

# 本地配置（含 API Key / Cookie），不要提交
config.yaml

# 运行时数据
data/uploads/
data/kb/.trash/

# 工具工作区（含本机绝对路径的会话记忆），如需把项目决策日志一并纳入版本管理，删掉这一行
.workbuddy-ai/

# macOS
.DS_Store
