---
type: source
tags:
- 知识库
- 维护
- markdown
- Karpathy
- 复用
- 检索
summary: LLM Wiki 的核心是让模型维护一个持久化的 markdown 知识库。知识编译只做一次，之后每次查询都复用已经整理好的页面，而不是重新做向量检索。Karpathy
  认为维护成本才是知识库失败的根本原因。
sources:
- raw/llm-wiki-的核心是让模型维护一个持久化的-markdown-知识库-知识编译只做一次-之后每次查询都复用已经整理.md
created: '2026-09-15'
updated: '2026-09-15'
title: LLM Wiki 的核心是让模型维护一个持久化的 markdown 知识库。知识编译只做一次，之后每次查询都复用已经整理
---

# LLM Wiki 的核心是让模型维护一个持久化的 markdown 知识库。知识编译只做一次，之后每次查询都复用已经整理

> 原始素材：`raw/llm-wiki-的核心是让模型维护一个持久化的-markdown-知识库-知识编译只做一次-之后每次查询都复用已经整理.md` · 类型 text

## 内容摘要

LLM Wiki 的核心是让模型维护一个持久化的 markdown 知识库。知识编译只做一次，之后每次查询都复用已经整理好的页面，而不是重新做向量检索。Karpathy 认为维护成本才是知识库失败的根本原因。

## 关键概念

知识库 [[维护]] markdown Karpathy 复用 检索 [[向量]] 编译 根本原因 [[查询]]
